#include <stdio.h>

int main() {
    int a,b;
    printf("enter the value pof a and b both are:");
    scanf("%d %d",&a,&b);
    if(a<=b)
    {
        printf("True");
    }
    else
    {
        printf("False");
    }
    return 0;
}